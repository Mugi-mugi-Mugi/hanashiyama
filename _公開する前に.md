# 公開する前に (★人がやること)

このフォルダは `python tools/make_release.py` が作ったものです。
**作業メモ (CLAUDE.md / 引継ぎ / docs/) と 生データ (data/raw/) は入っていません。**

## 1. LICENSE の名義
`LICENSE` の Copyright は **Mugi-mugi-Mugi** で入っています。変える時はここ。

## 2. 動くか確かめる
```
python tools/build.py && python tools/validate.py
```
そのあと `app/index.html` をブラウザで開く。

## 3. GitHub へ上げる
★git コマンドは 要りません。ブラウザに ★このフォルダの中身を ドラッグ&ドロップ します。
手順は 手元の `docs/手順/GitHubで公開する手順.md` に 書いてあります
(★そちらは 公開フォルダには 入りません)。
★上げるのは ★このフォルダの **中身** です。フォルダごとでは ありません。

## 入っていないものを確かめる
```
python tools/make_release.py --check
```
